version: "3.9"

services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data:ro

  frontend:
    build: ./frontend
    env_file:
      - ./frontend/.env
    ports:
      - "8501:8501"
    depends_on:
      - backend